pandas==2.2.2 # O la versión que uses localmente
numpy==1.26.4 # O la versión que uses localmente
scipy==1.13.1 # O la versión que uses localmente
scikit-learn==1.4.2 # O la versión que uses localmente
tensorflow==2.10.0 # ¡Versión clave! Esta es compatible con Python 3.8-3.11
keras==2.10.0 # Keras debe coincidir con TensorFlow
Flask==3.0.3 # Si usas Flask
python-dotenv==1.0.1 # O la versión que uses localmente
joblib==1.4.2 # O la versión que uses localmente
requests==2.32.3 # O la versión que uses localmente